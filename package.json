{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "description": "Full-stack personal portfolio website with React, Tailwind CSS, Node.js, and MongoDB",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "portfolio",
    "react",
    "tailwind",
    "node",
    "express",
    "mongodb",
    "full-stack"
  ],
  "author": "Your Name",
  "license": "MIT"
}
